import React, { Component } from 'react';

export default class Profile extends Component {
    render() {
        return (
            <p style={{ margin: 'auto' }} >Max Batty signed in :) </p>
        );
    }
}